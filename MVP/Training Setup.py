#Take the data from data.py and turn it into numerics that can be sent to a trainer by doing the following
#1 make all bids have their own associated token that has a vector of random weights
#2 have each card have an assocated token that has a vector of random weights
#turn the strategy decleration question each into a token that has a vector of random weights
